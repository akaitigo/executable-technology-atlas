# Contributing

Catalog、Schema、Completion Model、Mastery Contractは`reference-atlas-core`を正本とします。Portalへ個別技術知識を複製せず、固定Release envelopeとEvidenceを追加してください。署名・Digest・Schema・Publication Gateのいずれかに失敗するReleaseは公開Indexへ追加しません。
